services:
  - type: web
    name: bat-control
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    pullRequestDeployEnabled: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html